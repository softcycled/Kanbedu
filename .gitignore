# Dependencies
node_modules/

# Next.js
.next/
out/

# TypeScript build cache
tsconfig.tsbuildinfo

# Database
prisma/dev.db
prisma/dev.db-journal

# Env
.env
.env.local

# Misc
.DS_Store
*.log
.vercel
scripts

# Local-only working docs (roadmap, QA scripts, audit notes) — never committed
docs-local/

# Claude Code — ignore machine-specific files, track memory + skills (project knowledge)
.claude/settings.local.json
.claude/worktrees/
# .claude/memory/ is intentionally NOT ignored — it's the git-tracked knowledge base
.qa/
e2e/.auth/
playwright-report/
test-results/
